set -g prefix C-t
unbind C-b
bind C-t send-prefix
bind r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded!"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g status-style fg=#ffcfdf,bg=#771a4fn
bind t last-window
bind C-t last-window
